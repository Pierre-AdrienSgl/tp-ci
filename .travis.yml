
language: java

jdk:
  - openjdk8
  - openjdk14

  after_success:
- mvn clean site -X -Dgithub.site.dryRun=true

deploy:
  provider: releases
  api_key: "$GITHUB_API_KEY"
  skip_cleanup: true
  on:
    tags: true
    all_branches: true

